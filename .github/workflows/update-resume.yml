name: Update Resume Preview

on:
  push:
    branches:
      - main
    paths:
      - 'Subhabrata_Sahoo_Resume.pdf'

  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-preview:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install PDF tools
        run: |
          sudo apt-get update
          sudo apt-get install -y poppler-utils

      - name: Generate Preview in Hidden Assets Folder & Update README
        run: |
          # 1. Ensure hidden assets directory exists
          mkdir -p .github/assets

          # 2. Clean up old preview images across root and .github
          rm -f resume-preview-*.png Subhabrata_Sahoo_Resume_Preview*.png .github/assets/resume-preview-*.png

          # 3. Define image path inside .github/assets/
          IMAGE_PATH=".github/assets/resume-preview-${GITHUB_SHA::7}"

          # 4. Convert PDF to PNG inside .github/assets/
          pdftoppm -png -f 1 -singlefile Subhabrata_Sahoo_Resume.pdf "$IMAGE_PATH"

          # 5. Rebuild README.md pointing to the nested assets path
          cat > README.md <<EOF
          [![Resume Preview](./${IMAGE_PATH}.png)](./Subhabrata_Sahoo_Resume.pdf)

          **[Download Resume PDF](./Subhabrata_Sahoo_Resume.pdf)**
          EOF

      - name: Commit updated preview
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add -A

          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Update resume preview [skip ci]"
            git pull --rebase origin main
            git push origin main
          fi
